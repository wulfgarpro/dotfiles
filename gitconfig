[core]
	editor = nvim
[merge]
	tool = meld
[diff]
	tool = meld
[difftool]
	prompt = false
[pull]
	ff = true
[filter "lfs"]
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
